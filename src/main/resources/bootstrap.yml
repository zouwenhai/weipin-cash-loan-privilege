server:
    port: 8080

spring:
  application:
    name: yofishdk-cash-loan-privilege
  http:
    encoding:
      charset: UTF-8
      force: true
  profiles:
    active: dev
  cloud:
    refresh:
      refreshable: none
    config:
      profile: ${spring.profiles.active}
      discovery:
        enabled: true
        serviceId: yofishdk-cloud-config
  rabbitmq:
    listener:
      simple:
        acknowledge-mode: none
        retry.enabled: false
feign:
  hystrix:
    enabled: true
hystrix:
  command:
    default:
      execution:
        timeout.enabled: true
        isolation:
          strategy: SEMAPHORE
          semaphore.maxConcurrentRequests: 200
          thread.timeoutInMilliseconds: 120000
ribbon:
  MaxAutoRetries: 0
  MaxAutoRetriesNextServer: 0
  ServerListRefreshInterval: 5000
  ReadTimeout: 120000
  ConnectTimeout: 120000

eureka:
  client:
    serviceUrl.defaultZone: http://test.lb.gs.youyuwo.com:13115/eureka/,http://test.lb.gs.youyuwo.com:13116/eureka/,http://test.lb.gs.youyuwo.com:13117/eureka/
    register-with-eureka: false
    registry-fetch-interval-seconds: 5
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 10


---
eureka:
  client:
    serviceUrl.defaultZone: http://test.lb.gs.youyuwo.com:13115/eureka/,http://test.lb.gs.youyuwo.com:13116/eureka/,http://test.lb.gs.youyuwo.com:13117/eureka/
    register-with-eureka: true
spring:
  profiles: test

---
eureka:
  client:
    serviceUrl.defaultZone: http://10.0.40.14:13115/eureka/,http://10.0.40.14:13116/eureka/,http://10.0.40.14:13117/eureka/
    register-with-eureka: true
spring:
  profiles: pre

---
eureka:
  client:
    serviceUrl.defaultZone: http://172.20.20.101:13115/eureka/,http://172.20.20.102:13116/eureka/,http://172.20.20.103:13117/eureka/
    register-with-eureka: true
spring:
  profiles: prod